--TEST--
001: ORDERED
--TARGET--
aarch64
--ARGS--
-S
--CODE--
{
	double c = 0;
	l_1 = START(l_4);
	double x = PARAM(l_1, "x", 1);
	bool ret = ORDERED(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	fcmp d0, d0
	cset w0, vc
	ret
