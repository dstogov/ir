--TEST--
005: aarch64 constant SHL32 by 65 keeps immediates in range
--TARGET--
aarch64
--ARGS--
-O1 -S
--CODE--
{
	uint32_t c = 65;
	l_1 = START(l_4);
	uint32_t x = PARAM(l_1, "x", 1);
	uint32_t ret = SHL(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	lsl w0, w0, #1
	ret
