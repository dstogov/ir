--TEST--
015: aarch64 constant SAR32 by 65 keeps immediates in range
--TARGET--
aarch64
--ARGS--
-O1 -S
--CODE--
{
	int32_t c = 65;
	l_1 = START(l_4);
	int32_t x = PARAM(l_1, "x", 1);
	int32_t ret = SAR(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	asr w0, w0, #1
	ret
