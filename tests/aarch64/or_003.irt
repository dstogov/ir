--TEST--
003: or function
--TARGET--
aarch64
--ARGS--
-S
--CODE--
{
	uint64_t c = 0x40000000001; /* (1<<42) | 1 */
	l_1 = START(l_4);
	uint64_t x = PARAM(l_1, "x", 1);
	uint64_t ret = OR(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	mov x1, #1
	movk x1, #0x400, lsl #32
	orr x0, x0, x1
	ret
