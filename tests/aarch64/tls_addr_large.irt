--TEST--
TLS_ADDR with large offset
--TARGET--
aarch64-linux-sysv
--ARGS--
-O0 -S
--CODE--
func test(): uintptr_t
{
	l_1 = START(l_2);
	uintptr_t a = TLS_ADDR(-1, 5000);
	l_2 = RETURN(l_1, a);
}
--EXPECT--
test:
	sub sp, sp, #0x10
	mrs x0, tpidr_el0
	add x0, x0, #1, lsl #12
	add x0, x0, #0x388
	str x0, [sp]
	ldr x0, [sp]
	add sp, sp, #0x10
	ret
