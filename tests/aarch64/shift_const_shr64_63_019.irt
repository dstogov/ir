--TEST--
019: aarch64 constant SHR64 by 63 keeps immediates in range
--TARGET--
aarch64
--ARGS--
-O1 -S
--CODE--
{
	uint64_t c = 63;
	l_1 = START(l_4);
	uint64_t x = PARAM(l_1, "x", 1);
	uint64_t ret = SHR(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	lsr x0, x0, #0x3f
	ret
