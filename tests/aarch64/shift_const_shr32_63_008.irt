--TEST--
008: aarch64 constant SHR32 by 63 keeps immediates in range
--TARGET--
aarch64
--ARGS--
-O1 -S
--CODE--
{
	uint32_t c = 63;
	l_1 = START(l_4);
	uint32_t x = PARAM(l_1, "x", 1);
	uint32_t ret = SHR(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	lsr w0, w0, #0x1f
	ret
