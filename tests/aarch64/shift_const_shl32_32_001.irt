--TEST--
001: aarch64 constant SHL32 by 32 keeps immediates in range
--TARGET--
aarch64
--ARGS--
-O1 -S
--CODE--
{
	uint32_t c = 32;
	l_1 = START(l_4);
	uint32_t x = PARAM(l_1, "x", 1);
	uint32_t ret = SHL(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	ret
