--TEST--
011: aarch64 constant SAR32 by 32 keeps immediates in range
--TARGET--
aarch64
--ARGS--
-O1 -S
--CODE--
{
	int32_t c = 32;
	l_1 = START(l_4);
	int32_t x = PARAM(l_1, "x", 1);
	int32_t ret = SAR(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	ret
