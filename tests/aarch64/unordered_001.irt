--TEST--
001: UNORDERED
--TARGET--
aarch64
--ARGS--
-S
--CODE--
{
	float c = 0;
	l_1 = START(l_4);
	float x = PARAM(l_1, "x", 1);
	bool ret = UNORDERED(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	fcmp s0, s0
	cset w0, vs
	ret
