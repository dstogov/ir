--TEST--
001: ORDERED
--ARGS--
--emit-llvm
--CODE--
{
	double c = 0;
	l_1 = START(l_4);
	double x = PARAM(l_1, "x", 1);
	bool ret = ORDERED(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
define i1 @test(double %d2)
{
	%d3 = fcmp ord double %d2, %d2
	ret i1 %d3
}
