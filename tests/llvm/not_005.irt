--TEST--
NOT 005: float boolean NOT emits ordered zero comparison
--ARGS--
--emit-llvm
--CODE--
{
	l_1 = START(l_4);
	float x = PARAM(l_1, "x", 1);
	bool ret = NOT(x);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
define i1 @test(float %d2)
{
	%d3 = fcmp oeq float %d2, 0.0
	ret i1 %d3
}
