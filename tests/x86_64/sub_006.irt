--TEST--
006: sub function
--ARGS--
-S
--CODE--
{
	l_1 = START(l_4);
	double x = PARAM(l_1, "x", 1);
	double y = PARAM(l_1, "y", 2);
	double ret = SUB(y, x);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	subsd %xmm0, %xmm1
	movapd %xmm1, %xmm0
	retq 
