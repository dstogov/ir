--TEST--
004: eq function
--ARGS--
-S
--CODE--
{
	double c = 0;
	l_1 = START(l_4);
	double x = PARAM(l_1, "x", 1);
	bool ret = EQ(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	ucomisd 0x10(%rip), %xmm0
	setnp %al
	movl $0, %edx
	cmovnel %edx, %eax
	retq
