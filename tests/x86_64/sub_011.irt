--TEST--
011: sub function
--ARGS--
-S
--CODE--
{
	double c = 0.5;
	l_1 = START(l_4);
	double x = PARAM(l_1, "x", 1);
	double ret = SUB(c, x);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	movsd 0x10(%rip), %xmm1
	subsd %xmm0, %xmm1
	movapd %xmm1, %xmm0
	retq
