--TEST--
001: ORDERED
--TARGET--
x86_64
--ARGS--
-S
--CODE--
{
	double c = 0;
	l_1 = START(l_4);
	double x = PARAM(l_1, "x", 1);
	bool ret = ORDERED(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	ucomisd %xmm0, %xmm0
	setnp %al
	retq
