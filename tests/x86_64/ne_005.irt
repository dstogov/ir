--TEST--
005: ne function
--ARGS--
-S -mavx
--CODE--
{
	double c = 0;
	l_1 = START(l_4);
	double x = PARAM(l_1, "x", 1);
	bool ret = NE(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	vucomisd 0x10(%rip), %xmm0
	setp %al
	movl $1, %ecx
	cmovnel %ecx, %eax
	retq
