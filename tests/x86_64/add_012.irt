--TEST--
012: add function
--ARGS--
-S -mavx
--CODE--
{
	double c = 0.5;
	l_1 = START(l_4);
	double x = PARAM(l_1, "x", 1);
	double ret = ADD(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	vaddsd 8(%rip), %xmm0, %xmm0
	retq
