--TEST--
005: le function
--ARGS--
-S -mavx
--CODE--
{
	double c = 0;
	l_1 = START(l_4);
	double x = PARAM(l_1, "x", 1);
	bool ret = LE(x, c);
	l_4 = RETURN(l_1, ret);
}
--EXPECT--
test:
	vxorpd %xmm7, %xmm7, %xmm7
	vucomisd %xmm0, %xmm7
	setae %al
	retq
